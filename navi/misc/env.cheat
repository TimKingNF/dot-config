% shell env operation

# 查找执行文件的所在位置
which <cmd>

;# 查看链接库/执行文件的位置信息
;whereis <cmd>

# 查看执行文件的链接库 ( Linux )
ldd <bin>

# 查看执行文件的链接库 ( OSX )
otool -L <bin>

# 设置时区
timedatectl set-timezone Asia/Shanghai